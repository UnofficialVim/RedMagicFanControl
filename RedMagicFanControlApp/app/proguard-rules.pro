# Add project specific ProGuard rules here.
# This module currently ships with isMinifyEnabled = false for the
# release build type, so these rules are not applied yet. Flip that
# flag once the domain-socket protocol and daemon integration are final.
