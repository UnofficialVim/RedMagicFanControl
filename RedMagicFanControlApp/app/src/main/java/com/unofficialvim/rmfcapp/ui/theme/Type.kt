package com.unofficialvim.rmfcapp.ui.theme

import androidx.compose.material3.Typography

// Material3's default type scale. Customize here if the app needs a
// distinct look; left as-is so the app still looks "stock Google app".
val AppTypography = Typography()
